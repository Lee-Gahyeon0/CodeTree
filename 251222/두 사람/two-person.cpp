#include <iostream>
using namespace std;

int main() {
    int a,c;
    char b,d;

    cin >> a >> b >> c >> d;
    if((a>=19 && c>=19) ||0 ( b =='M' && d == 'M')){
        cout << "1";
    }

    return 0;
}